---
applyTo: "src/components/**/*.{tsx}"
---
- Use functional React components with hooks only; avoid class components.
- Use Tailwind CSS for styling, avoid inline styles.
- Every componentn must have a defined Props interface.